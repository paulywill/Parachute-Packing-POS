<%= form_for(@packer) do |f| %>
  <% if @packer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@packer.errors.count, "error") %> prohibited this packer from being saved:</h2>

      <ul>
      <% @packer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :p_name %><br />
    <%= f.text_field :p_name %>
  </div>
  <div class="field">
    <%= f.label :p_cspa_date %><br />
    <%= f.date_select :p_cspa_date %>
  </div>
  <div class="field">
    <%= f.label :p_team %><br />
    <%= f.check_box :p_team %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
